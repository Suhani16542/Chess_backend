/**
 * This directory is reserved for shared TypeScript type definitions.
 *
 * Examples of what to place here:
 *  - Express Request augmentation (e.g., attaching user session to req)
 *  - Shared DTO (Data Transfer Object) interfaces
 *  - Third-party module declaration overrides
 *
 * Example: src/types/express.d.ts
 * --------------------------------
 * declare global {
 *   namespace Express {
 *     interface Request {
 *       user?: { id: string; role: string };
 *     }
 *   }
 * }
 * export {};
 */
